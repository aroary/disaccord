const Discord = require('discord.js');
module.exports = {
    config:{
        name: "name"
    },
    /**
     * @param {Discord.Client} client - Your client
     * @template
     */
    run: (client/*, other events*/) => {
        // code
    }
};